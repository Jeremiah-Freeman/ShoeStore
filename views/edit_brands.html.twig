<!DOCTYPE html>
<html>
<head>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://fonts.googleapis.com/css?family=Josefin+Sans" rel="stylesheet">

    <meta charset="utf-8">
    <title>Update and Delete {{brand.getBrandName}}</title>
</head>
<body>
    <div class="container">
        <div id="jumb" class="jumbotron">
            <p> Update and Delete {{brand.getBrandName}}?</p>
        </div>
        <div id="addbrand" class="col-md-4">
            <div class="form-group">
                <form action="/brand/{{ brand.getId }}" method="post">
                    <input name="_method" type="hidden" value="patch">
                    <label class="form-label" for="name">Rename brand:</label>
                    <input class="form-control" id="name" name="name" type="text">
                    <button  class="btn btn-primary" type="submit">Update</button>
                </form>
                <form class="" action="/deletebrand/{{ brand.getBrandId }}" method="post">
                    <input type="hidden" name="_method" value="delete">
                    <button class="btn btn-primary"  type="submit">delete</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
